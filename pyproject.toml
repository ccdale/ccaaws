[project]
name = "ccaaws"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ccdale", email = "chris.charles.allison+git@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.89",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
